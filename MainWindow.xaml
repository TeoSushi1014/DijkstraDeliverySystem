<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DijkstraDeliverySystem.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DijkstraDeliverySystem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DijkstraDeliverySystem">

    <Grid Padding="16" RowSpacing="12" ColumnSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Case" />
            <ComboBox x:Name="TestCaseBox" Width="130" SelectedIndex="0" SelectionChanged="TestCaseBox_SelectionChanged">
                <ComboBoxItem Content="TC1 - Reachable" />
                <ComboBoxItem Content="TC2 - Multi-path" />
                <ComboBoxItem Content="TC3 - Unreachable" />
                <ComboBoxItem Content="TC4 - Source=Target" />
                <ComboBoxItem Content="TC5 - 50 Nodes" />
            </ComboBox>

            <TextBlock VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Source" />
            <TextBox x:Name="SourceNodeBox" Width="80" Text="0" />

            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Target" />
            <TextBox x:Name="TargetNodeBox" Width="80" Text="7" />

            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Iterations" />
            <NumberBox x:Name="IterationBox"
                       Width="120"
                       Value="30"
                       Minimum="1"
                       Maximum="1000"
                       SmallChange="1"
                       SpinButtonPlacementMode="Inline"
                       AcceptsExpression="False" />

            <Button x:Name="SolveButton" Margin="12,0,0,0" Click="SolveButton_Click" Content="Run Dijkstra" />
            <Button x:Name="CancelButton" Margin="8,0,0,0" Click="CancelButton_Click" Content="Cancel" IsEnabled="False" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <Border Width="14" Height="14" CornerRadius="7" Background="#0E7490" VerticalAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Text="Normal node" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <Border Width="14" Height="14" CornerRadius="7" Background="#22C55E" VerticalAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Text="Source (subtle pulse)" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <Border Width="14" Height="14" CornerRadius="7" Background="#F59E0B" VerticalAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Text="Target (strong pulse)" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <Border Width="14" Height="14" CornerRadius="7" Background="#DC2626" VerticalAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Text="Path node (light pulse)" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="8">
            <Canvas x:Name="PathCanvas" MinHeight="420" />
        </Border>

        <StackPanel Grid.Row="3" Spacing="8">
            <ProgressBar x:Name="BenchmarkProgressBar" Minimum="0" Maximum="100" Value="0" Height="6" Visibility="Collapsed" />
            <TextBlock x:Name="StatusTextBlock" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
